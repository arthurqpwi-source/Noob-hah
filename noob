-- Noob prime v16.0 - Somente Função Roube
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local ToggleButton = Instance.new("TextButton")
local RoubeBtn = Instance.new("TextButton")
local CloseBtn = Instance.new("TextButton")

-- Setup da UI (Não reseta ao morrer)
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

-- Botão Flutuante para abrir o menu
ToggleButton.Name = "NP_Toggle"
ToggleButton.Parent = ScreenGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(180, 0, 50)
ToggleButton.Position = UDim2.new(0, 10, 0.5, -22)
ToggleButton.Size = UDim2.new(0, 60, 0, 45)
ToggleButton.Text = "ROUBE"
ToggleButton.TextColor3 = Color3.new(1, 1, 1)
ToggleButton.Font = Enum.Font.SourceSansBold
ToggleButton.Visible = false

-- Menu Principal
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.BorderSizePixel = 2
MainFrame.BorderColor3 = Color3.fromRGB(180, 0, 50)
MainFrame.Size = UDim2.new(0, 200, 0, 150)
MainFrame.Position = UDim2.new(0.4, 0, 0.4, 0)
MainFrame.Active = true
MainFrame.Draggable = true

-- Título
Title.Parent = MainFrame
Title.Size = UDim2.new(1, 0, 0.3, 0)
Title.Text = "NOOB PRIME"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 18

-- Botão Fechar
CloseBtn.Parent = MainFrame
CloseBtn.Size = UDim2.new(0.2, 0, 0.2, 0)
CloseBtn.Position = UDim2.new(0.8, 0, 0, 0)
CloseBtn.Text = "X"
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
CloseBtn.TextColor3 = Color3.new(1, 1, 1)
CloseBtn.BorderSizePixel = 0

-- Botão ROUBE (A Função Principal)
RoubeBtn.Parent = MainFrame
RoubeBtn.Size = UDim2.new(0.8, 0, 0.4, 0)
RoubeBtn.Position = UDim2.new(0.1, 0, 0.45, 0)
RoubeBtn.Text = "EXECUTAR ROUBE"
RoubeBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 50)
RoubeBtn.TextColor3 = Color3.new(1, 1, 1)
RoubeBtn.Font = Enum.Font.SourceSansBold
RoubeBtn.TextSize = 16

-- Lógica de abrir e fechar
CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    ToggleButton.Visible = true
end)

ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = true
    ToggleButton.Visible = false
end)

-- Execução do Script Externo
RoubeBtn.MouseButton1Click:Connect(function()
    RoubeBtn.Text = "CARREGANDO..."
    task.wait(0.5)
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Ninja10908/S4/refs/heads/main/Kurdhub"))()
    RoubeBtn.Text = "EXECUTAR ROUBE"
end)
